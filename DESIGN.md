# 设计与选型

这份文档解释**这套记忆库为什么长这样**。第 2 章从零讲清楚所有术语，不需要检索或
机器学习背景；第 3 章往后才是具体设计和取舍。想看怎么用，去 `README.md`。

本文件在库根目录，已被 L1/memory-doc 索引，所以 `mem recall "为什么用 trigram"`
能查到自己。

---

## 1. 要解决的问题

Agent 每天产生三类东西，但它们的生命周期完全不同：

| 类型 | 例子 | 问题 |
|---|---|---|
| 协作记忆 | "提交前先跑 lint，别等 CI 挂" | 散在各 project 目录，跨项目不共享 |
| 领域知识 | 踩坑页、SOP、改动记录 | 攒在某个笔记目录里，只能靠人翻 |
| 历史会话 | "上次那个缓存问题怎么绕的" | 埋在几百个 jsonl 里，grep 不动 |

三者的共同缺口是**检索**，不是存储。所以这套东西的定位是**统一检索层**，
不是又一个笔记系统：事实源保持原样、原地不动，索引是可以随时删掉重建的派生产物。

**不解决**的事：不做知识图谱，不做自动摘要总结，不做常驻服务，不做多机同步。
一个 CLI，冷启动 0.7 秒出结果，够用。

---

## 2. 先补概念

这一章把后面要用的名词全部讲一遍。已经熟悉全文检索和向量检索的可以直接跳到第 3 章。

### 2.1 检索的基本单位是"块"，不是文件

直觉上检索是"找到那个文件"，但文件太大了——一篇 5000 字的踩坑页里可能记了七八件事，
把整篇当成一个检索单位，命中了也等于没命中，你还得自己在里面找。

所以索引前先**切块（chunk）**：一篇文档切成若干段，每段 600 字左右，
每段单独建索引、单独参与排序。检索返回的是块，直接就是你要看的那几行。

本库现在的规模：**约 3200 块**。`mem doctor` 会按层列出来。

一个块除了正文，还带着它的出身信息：

```
块 #1893
  layer        L2                          ← 哪一层（L1 记忆 / L2 知识 / L3 会话）
  source       pitfalls                    ← 哪个源
  source_path  ~/notes/pitfalls/xx.md      ← 原文在哪
  title        缓存不失效踩坑               ← 文档标题
  heading_path 排查 / 并发度                ← 在文档里的哪一节（标题路径）
  text         "……"                        ← 正文
```

后面说的"命中"、"排序"、"融合"，操作对象全是块。

### 2.2 关键词检索是怎么工作的

#### 倒排索引

关键词检索的底子是**倒排索引**：不是遍历每个块去看含不含某个词，而是反过来预先建一张表——
**词 → 包含它的块号列表**。

```
"缓存"  →  [12, 88, 431, 1893, ...]
"超时"  →  [88, 907, ...]
```

查"缓存 超时"时，取两个列表求交集就完了，不用碰正文。**FTS5** 就是 SQLite 内建的
这么一个全文检索模块（Full-Text Search 第 5 版），本库用它建了一张 `chunks_fts` 虚拟表。

#### 分词器与 trigram

建这张表的前提是**把文本切成词**，干这活的叫**分词器（tokenizer）**。英文很容易，
按空格切就行。中文没有空格，这是全部麻烦的来源。

FTS5 自带的默认分词器 `unicode61` 就是按空格和标点切的。喂给它一句中文：

```
"日志检索超时重试"   →   ["日志检索超时重试"]      ← 整句一个词
```

于是查"检索超时"永远查不到——表里只有"日志检索超时重试"这一个词条，
和你的查询词不相等。**中文用 `unicode61` 等于全文检索不可用**。

FTS5 另外提供了一个 `trigram` 分词器：**不理解词，就按每 3 个字符切一刀，逐字滑动**：

```
"日志检索超时"  →  "日志检" "志检索" "检索超" "索超时"
                    └─每次只往右挪一个字，所以相邻片段是重叠的
```

（tri = 三，gram = 片段，所以叫 trigram，中文一般说"三元组"或"3-gram"。）

这样查"检索超时"时，把查询也切成 `"检索超" "索超时"`，就能在表里查到了。
**代价**是索引变大（每个字都产生一个词条），好处是**完全不依赖词典**——
业务生造词、内部缩写、项目黑话照样能命中，因为它压根不需要认识这个词。

#### df（文档频率）

`df` 是 document frequency 的缩写，**指某个词片在多少个块里出现过**。这是个纯统计量：

```
"缓存穿"  df = 210     ← 210 个块里出现过
"RetryT"  df = 3       ← 只有 3 个块有，很稀有
"的问题"  df = 1150    ← 三分之一的块都有，等于没说
"索超时"  df = 0       ← 一个块都没有，这个片段跨了词边界
```

`df == 0` 的词片是**垃圾**：查它一定查不到东西，白白拖慢查询。
`df` 特别大的词片是**没有区分度的**：它谁都能匹配，等于没有筛选作用。
本库的做法是把这两头都丢掉，只留中间那些"够稀有、又确实存在"的词片（见 4.2）。

#### bm25

一个查询可能命中几百个块，得排序。**bm25** 是全文检索领域的标准打分公式，
FTS5 内建了它。它的直觉是三条：

- 查询词在这个块里出现得越多 → 分越高
- 这个词本身越稀有（df 越小）→ 这次命中越值钱
- 块越长 → 打个折（长文本本来就更容易蒙中）

本库直接用 `ORDER BY bm25(chunks_fts)` 拿排序，没有自己造轮子。

### 2.3 语义检索是怎么工作的

关键词检索有个死穴：**你必须猜中原文用的词**。原文写的是"缓存没刷新"，
你搜"数据不一致"，一个字都对不上，倒排索引救不了你。

语义检索解决的就是这个。

#### 嵌入与向量

**嵌入模型（embedding model）** 是一个神经网络，输入一段文本，输出一串固定长度的小数：

```
"缓存没刷新"      →  [0.031, -0.117, 0.204, ..., 0.008]
"数据不一致"      →  [0.028, -0.109, 0.221, ..., 0.011]     ← 数值很接近
"打印机纸张尺寸"  →  [-0.155, 0.302, -0.041, ..., 0.190]    ← 差得远
```

这串数就叫**向量（vector）**。模型是拿海量语料训出来的，训练目标就是：
**意思相近的文本，输出的数也相近**。它认识"缓存"和"数据"的关系，
不需要你猜中原文的用词。

#### 512 维是什么意思

**"512 维"就是这串数有 512 个**，仅此而已，没有别的玄机。

为什么不是 2 个？因为 2 个数只能表达平面上的一个点，能区分的"意思"太少了——
几千个块挤在一张纸上，全糊成一团。512 个数相当于一个 512 坐标轴的空间，
容量足够把不同的语义摆开。

维度是**模型定死的**，`bge-small-zh-v1.5` 就是 512 维，不是我们能选的参数。
换模型必须重建整个索引，因为 512 维的旧向量和新模型的向量根本不在一个空间里，
算出来的相似度毫无意义。

存储上，一个向量 = 512 个 float32 = 512 × 4 字节 = 2 KB。
约 3200 块 × 2 KB ≈ **6.6 MB**，整个库的向量就这么点，能一次全读进内存。

#### 余弦相似度

有了向量，"两段文本像不像"就变成了"两串数像不像"，用**余弦相似度**衡量。

把向量看成从原点射出的一支箭，余弦相似度就是**两支箭夹角的余弦值**：

```
夹角 0°   → 余弦 = 1.0    方向完全一致，意思最接近
夹角 90°  → 余弦 = 0      毫无关系
夹角 180° → 余弦 = -1.0   完全相反
```

用夹角而不是用距离，是因为**只关心方向，不关心箭有多长**——文本长短不该影响相似度判断。
代码里所有向量存进去之前都做了**归一化（normalize）**：统一拉成长度 1。
向量长度都是 1 之后，余弦值就等于两个向量对应位置相乘再求和（点积），一行代码的事。

#### "numpy 暴力余弦"是什么意思

检索时要拿查询向量和**全库每一个块**的向量都算一遍余弦，然后取最高的几个。
逐个算一遍、一个不落，这就叫**暴力（brute force）**检索。

听起来很慢，实际不是：约 3200 个块的向量是一个 3200×512 的数字表格，
**numpy**（Python 的数值计算库，底层是 C 和 SIMD 指令）把这件事变成一次矩阵乘法，
**约 1 毫秒**跑完。

它的反面是 **ANN（近似最近邻）**——先给向量建索引，检索时只看一小部分候选，
牺牲一点准确率换速度。数据量大时必须用，本库这个量级用它是纯亏：
建索引的开销、加载扩展的开销、准确率的损失，全都白付。

#### 各向异性与全局均值向量

这是本库语义腿能不能用的关键，也是最不直觉的一点。

理想情况下，全库向量应该均匀撒在整个空间里，相关的挨着、不相关的离得远。
但 bge 这类模型有个已知毛病叫**各向异性（anisotropy）**——所有向量都挤在空间的一个
**窄锥**里，方向高度雷同。后果是**任意两段文本的余弦都很高**：

```
实测本库：全部相似度落在 0.64 ~ 0.72 之间
          最相关的那条 0.72，毫不相关的那条 0.67
          差 0.05，排序信号几乎被淹没
```

修法：把这个锥的**中心挪到原点**。

**全局均值向量**就是"全库所有向量的平均"——512 个位置各自求平均，得到一个新的
512 维向量，它指向那个锥的中心方向。本库在每次索引结束时算好，
存进数据库的 `meta_vec` 表，键名 `center`。

检索时，文档向量和查询向量**都减去这个均值再重新归一化**：

```
减之前：所有箭都朝着东偏北 30° 的方向，彼此夹角只有几度
减之后：以那个方向为新原点，箭朝四面八方散开，夹角拉开
```

相对差异被放大，排序才有意义。成本只有索引时一次求平均，
没有训练、没有额外的模型、检索时多两次减法。

（严格做法叫 whitening，还要算协方差矩阵做变换。本库只做了减均值这一半，
因为剩下那一半要多存一个 512×512 的矩阵、每次检索多一次矩阵乘，
在这个数据量上收益边际——见 5. 选型总表。）

### 2.4 两条腿，以及怎么合并

上面两种检索，本文档一律叫**"腿"**——一条**关键词腿**（倒排索引那条），
一条**语义腿**（向量那条）。就是个通道的比喻，各走各的，最后合并。

`mem recall` 的每条结果行首都标了它是哪条腿捞上来的：

```
1. [L2/pitfalls KV] 缓存不失效踩坑 › 排查
                ^^
                K = 关键词腿命中    -K 位置为 - 表示这条腿没捞到它
                V = 语义腿命中
```

`KV` 双命中的可信度最高；只有 `K-` 的多半是字面匹配；只有 `-V` 的是语义联想，
偶尔会是擦边球。

#### 为什么不能直接把两个分数相加

因为**两个分数不可比**。bm25 是个没有上界的正数，随查询词数量剧烈变化；
余弦被限制在 -1 到 1。把 "bm25=8.3" 和 "余弦=0.71" 加起来是没有意义的运算。
先归一化再加权也试过，实测不稳——bm25 的分布本身就随查询漂移。

#### RRF：只看名次，不看分数

**RRF（Reciprocal Rank Fusion，倒数排名融合）** 绕开了这个问题：
**扔掉分数，只用名次**。

每条腿各自排好序，一个块在某条腿里排第几名，就拿一个由名次决定的分：

```
score(块) =  Σ    1 / (k + 名次)          k = 60
          各条腿
```

举例（k=60）：

| 块 | 关键词腿名次 | 语义腿名次 | 得分 |
|---|---|---|---|
| A | 第 1 | 没捞到 | 1/61 = 0.0164 |
| B | 第 5 | 第 3 | 1/65 + 1/63 = 0.0313 |
| C | 没捞到 | 第 1 | 1/61 = 0.0164 |

B 虽然在两条腿里都不是第一，但**两条腿都认它**，融合后反超。这正是想要的效果。

`k = 60` 的作用是**压平头部差距**：分母加了 60，第 1 名（1/61）和第 3 名（1/63）
只差 3%，没有哪条腿的 top-1 能独断结果。k 调小则头部更强势，调大则更均衡。

### 2.5 术语速查

| 术语 | 一句话 |
|---|---|
| 块 / chunk | 检索的基本单位，一篇文档切出来的一段（约 600 字） |
| 倒排索引 | "词 → 含它的块号列表"的预建表，全文检索的底子 |
| FTS5 | SQLite 内建的全文检索模块，本库用它存倒排索引 |
| 分词器 | 把文本切成词条的组件；中文是难点 |
| unicode61 | FTS5 默认分词器，按空格标点切，**不切中文** |
| trigram | FTS5 的另一个分词器，按每 3 字符逐字滑动切，不依赖词典 |
| df | 文档频率，某词片出现在多少个块里；df=0 即该片段不存在 |
| bm25 | 全文检索的标准打分公式，FTS5 内建 |
| 嵌入模型 | 把文本转成一串数的神经网络 |
| 向量 | 那串数；本库每块 512 个 float32 = 2 KB |
| 512 维 | 就是"这串数有 512 个"，模型定死，换模型必须重建索引 |
| 归一化 | 把向量统一拉成长度 1，只留方向信息 |
| 余弦相似度 | 两个向量夹角的余弦，1 = 最像，0 = 无关 |
| 暴力检索 | 和全库每个向量都算一遍，一个不落；反面是 ANN 近似检索 |
| numpy | Python 数值计算库，把暴力检索变成一次矩阵乘法，约 1 ms |
| 各向异性 | 模型输出的向量全挤在窄锥里，导致相似度区分不开 |
| 全局均值向量 | 全库向量的平均，减掉它就能把窄锥拉开 |
| 关键词腿 / 语义腿 | 本文档对两条检索通道的叫法 |
| RRF | 只看名次不看分数的融合方法，解决两条腿分数不可比 |
| 召回深度 | 融合前每条腿各取多少个候选，本库 30 |

---

## 3. 整体链路

```
             ┌─ L1  memory/**.md          （协作记忆，人工审核后合入）
 事实源 ─────┼─ L2  <你的笔记目录>/**.md   （领域知识，原地只读）
             └─ L3  *.jsonl               （Claude / Codex 会话）
                        │
                        ▼
              iter_documents()            sources.py  统一成 Document
                        │
                        ├─ 脱敏           redact.py   路径跳过 + 正则打码
                        ├─ 切块           chunk.py    按标题层级 → (heading_path, text)
                        └─ 嵌入           embed.py    文本 → 512 维向量
                        │
                        ▼
                 .index/memory.db         store.py
                   ├─ chunks              块正文 + 元数据
                   ├─ chunks_fts          FTS5 trigram 倒排索引
                   ├─ vectors             512 维 float32 向量
                   └─ meta_vec.center     全局均值向量
                        │
                        ▼
      ┌──────── 关键词腿 ────────┐  ┌──────── 语义腿 ────────┐
      │ 查询切 3-gram            │  │ 查询 → 512 维向量      │
      │ → 按 df 筛掉垃圾和废词   │  │ → 减全局均值           │
      │ → FTS5 匹配 → bm25 排序  │  │ → 全库余弦 → 取前 30   │
      │ → 取前 30                │  │                        │
      └──────────┬───────────────┘  └───────────┬────────────┘
                 └────────── RRF 融合 ──────────┘        search.py
                                │
                                ▼
                       top-k 结果（标注 K / V）
```

---

## 4. 具体设计

有了第 2 章的概念，这里讲每个环节**为什么这么定**。

### 4.1 为什么必须两条腿

真实查询分两类，任何单一手段都会在另一类上崩掉：

- **精确标识**：表名、工单号、类名，比如 `t_order_ext`、`task-1024`、`RetryTemplate`。
  语义腿对这类查询很差——嵌入模型没见过这些标识符，压出来的向量基本是噪声。
- **自然语言问法**：「这个接口为什么偶尔超时」。文档里可能一次都没出现这个词组，
  关键词腿命中恒为 0，只有语义能捞出来。

砍掉任何一条都会有一整类查询直接报废，所以是"两条都跑，再融合"。

### 4.2 关键词腿：stride=1 与 df 过滤

倒排索引那一侧是 SQLite 自动做的（文档进库时自动切 trigram），
但**查询这一侧要我们自己把中文切成 3-gram**。这里有个必须踩过一次才知道的坑：

```
查询："日志检索超时重试"

每 3 个字切一刀，不重叠（stride=3）：
    日志检 / 索超时 / 重试
             ^^^^^^
             跨词边界的垃圾片段，语料里几乎不出现 → df = 0 → 命中恒为 0

逐字滑动（stride=1）：
    日志检 / 志检索 / 检索超 / 索超时 / 超时重 / 时重试
     ✓        ✓                        ✓
     真实词片（日志 / 检索 / 超时）一定在候选里
```

**必须 stride=1**，否则一刀切错，整条查询就废了。

stride=1 的代价是候选词爆炸（n 个字出 n-2 个片段），所以后面接一道 **df 过滤**
（`search.py: build_fts_query`）：

- `df == 0` 的丢掉 —— 上面那些垃圾片段自动被清掉，**不用维护停用词表**
- `df > 总块数 × 25%` 的丢掉 —— "的问题"、"怎么办"这类没有区分度
- 剩下的按 **df 从小到大**（越稀有越靠前）排序，取前 12 个，用 `OR` 连起来丢给 FTS5

这道过滤是**自适应**的：库里内容变了，什么词算"太常见"跟着变，不需要人工维护词表。

### 4.3 语义腿：模型选型

**`BAAI/bge-small-zh-v1.5`**，512 维，ONNX 格式，约 90 MB，跑在 CPU 上：

| 备选 | 为什么不选 |
|---|---|
| OpenAI / 通义 等 API 嵌入 | 库里全是内部业务内容和会话原文，**不能出网**；索引 3000+ 块还要按量计费 |
| bge-large / bge-m3 | 大 4~8 倍，冷启动和索引时间都上去了，这个量级下召回收益看不出来 |
| **bge-small-zh-v1.5（选中）** | 中文语料训练，CPU 上索引 3000 块几十秒，全离线 |

选**中文专用**而不是多语言模型，是因为库里 95% 是中文；混着的英文标识符
本来就交给关键词腿兜底了。

去全局均值那一步的原理见 2.3，那是这条腿能不能用的分水岭，不是可选优化。

### 4.4 切块：按标题切，标题拼进嵌入文本

块太大则一块里塞了几件不相关的事，向量被平均成四不像；块太小则丢上下文，
"改成 3 就行了"这种句子单独拿出来毫无意义。

策略（`chunk.py`）：

1. **按 Markdown 标题层级切**，维护标题栈得到 `heading_path`（`踩坑 / 缓存 / 失效策略`）
2. 段落打包到 `chunk_target_chars = 600` 附近，超长单段按 `chunk_max_chars = 1200` 硬切
3. 代码围栏内的 `#` 不当标题（否则 shell 注释会把块切碎）

**一个容易漏掉的关键细节**：送进嵌入模型的文本不是块正文本身，
而是 `f"{title} / {heading}\n{text}"`（`cli.py: flush`）。

一个只写着"改成 3 就行了"的块，本身没有任何可检索的语义；
拼上 `缓存不失效踩坑 / 排查 / 并发度` 之后，它的向量才落在正确的位置。
存进 `chunks.text` 的仍然是原文，所以展示不受影响。

### 4.5 会话层：先过滤再索引

L3 是信噪比最差的一层，两道过滤是必须的：

- **Codex**：1001 个 rollout 文件里 840 个是 subagent 自动化会话（审批评估占 797 个），
  正文全是 `APPROVAL REQUEST` / `TRANSCRIPT DELTA` 模板。全量索引进去，
  这些高度重复的模板文本会在向量空间里形成一个巨大的吸引子——它们彼此极像，
  数量又多，几乎每次语义检索都会被捞上来，把真内容全挤掉。
  只收 `session_meta.thread_source == "user"`，过滤后 158 个会话 / 269 个回合。
- **Claude**：丢掉 `isMeta` / `isSidechain`，丢掉 `tool_use` / `tool_result` 块，
  正则剥掉 `<system-reminder>`、`<command-name>` 这类系统注入的包裹。

**切分单位是"回合"（一次 user 提问 + 其后的 assistant 回复），不是整个会话文件。**
一个 8 MB 的 transcript 当成一个块，命中它等于没命中；按回合切，
`doc_id = <路径>#<序号>`，结果能直接定位到具体那一问。

---

## 5. 选型总表

| 决策点 | 选中 | 主要备选 | 理由 |
|---|---|---|---|
| 存储 | SQLite（FTS5 + BLOB） | Chroma / LanceDB / Qdrant | 零部署、无常驻进程、单文件可删可重建；向量库要么带服务要么带一堆依赖 |
| 向量检索 | numpy 暴力余弦 | sqlite-vec、faiss、HNSW | 约 3200 块 × 512 维 ≈ 6.6 MB，全量矩阵乘约 1 ms，比加载 C 扩展还快；ANN 在这个量级是纯负收益 |
| 中文分词 | FTS5 trigram | jieba + unicode61 | 零依赖、不依赖词典、业务生造词照样命中；jieba 还要保证索引和查询分词版本严格一致 |
| 嵌入 | 本地 ONNX bge-small-zh | API 嵌入、更大的本地模型 | 内容不能出网；小模型在这个量级够用 |
| 各向异性 | 减全局均值 | 完整 whitening、不处理 | 不处理则相似度全挤在 0.64~0.72 排不出序；完整 whitening 要多存 512×512 矩阵，收益边际 |
| 融合 | RRF 等权 | 归一化加权、纯向量、纯关键词 | 分数不可比；加权 lexical 实测反而变差（高频 gram 拉进来的噪声也跟着加权） |
| 重排 | 无 | cross-encoder reranker | 再加一个模型换 200 ms+ 延迟，top-8 场景收益不明显；留作后续 |
| 事实源 | Markdown 原地不动 | 全量导入统一库 | L2 有 Obsidian vault、`.kb-state`、Windows 扫描流水线依赖现路径，搬家等于拆三条流水线 |
| 自动写入 | 一律落 staging | 直接写事实源 | 模型起草的记忆必须过人眼，写错的记忆比没记忆更糟 |
| 跨平台 | WSL2 单一事实源 + 单向导出 | 两套运行时、双向同步 | 双向同步必然冲突；Windows 侧调 `wsl.exe` 即可 |
| 运行时 | 独立 venv（Python 3.13） | 系统 python3 | 系统 3.14 无 pip 无 ensurepip，任何 fallback 都会静默退化 |

---

## 6. 参数手册

`config.json` 里的旋钮，以及**什么情况下才该动它**：

| 参数 | 当前 | 什么时候调 |
|---|---|---|
| `index.chunk_target_chars` | 600 | 结果块太碎、缺上下文 → 调大；一块里混了几件事 → 调小 |
| `index.chunk_max_chars` | 1200 | 长表格 / 长代码块被硬切坏了 → 调大 |
| `index.candidate_depth` | 30 | 明知道有但排不进 top-k → 调大到 50。只多捞长尾，不改变头部结果 |
| `index.rrf_k` | 60 | 想让某条腿的 top-1 更强势 → 调小；想更均衡 → 调大 |
| `index.backend` | `numpy` | 超过约 10 万块、单次检索 >200 ms 时才考虑换 ANN |
| `embedding.model` | bge-small-zh-v1.5 | **换模型必须 `mem index --full`**：维度和语义空间都变了，旧向量全作废 |
| `session.min_chars` | 40 | 短问答（"继续"、"好的"）污染结果 → 调大 |
| `session.max_chars` | 4000 | 长回合被截断丢结论 → 调大，代价是块数上涨 |
| `MAX_DF_RATIO` | 0.25（`search.py` 内） | 关键词腿噪声太多 → 调小到 0.15 |
| `MAX_TERMS` | 12（`search.py` 内） | 长查询丢词 → 调大，代价是 FTS 查询变慢 |

改完 `redaction` 规则必须 `mem index --full`——脱敏发生在索引时，
旧库里已经存进去的内容不会因为规则变严而自动重新打码。

---

## 7. 已知边界

- **没有时间衰减**：三年前的结论和昨天的结论排序上一视同仁。L2 靠 `verified_at`
  和 `status: stale` 人工标注兜底，L3 靠结果里的时间戳自己判断。开场注入是例外——
  那里按类型优先级 + mtime 选条目，见 `hooks/select-index.py`。
- **只按文档去重，不按内容去重**：同一篇文档在结果里最多占 `max_chunks_per_doc` 块
  （默认 2，配额席位只给 1 块）；但同一件事在 L2 记一遍、在会话里又说一遍，
  仍然会各占一个结果位。
- **分层配额是先验，不是相关性**：没指定 `--layer` 时 L1/L2 各有保底席位，
  代价是纯会话类问题（"上次那个报错"）的 top-k 里会掺 1~2 条层内最优但整体不算最相关的
  条目。阈值 `recall.quota_min_sim` 挡掉的是完全无关的凑数，不保证席位内容一定有用。
- **没有重排**：top-8 里通常有 1~2 条是语义腿捞来的擦边球。
- **增量索引按 mtime**：`touch` 一个没改内容的文件会触发重新嵌入；反过来，
  改了内容但 mtime 没变（罕见）会漏掉。有疑虑就 `--full`。
- **`prune_missing` 只在全量遍历时生效**：带 `--layer` 的增量索引不清理失效文档，
  删了文件后要跑一次不带 `--layer` 的 `mem index`。
- **L2 / L3 是线索不是事实**：与当前代码、配置、数据库冲突时一律以现状为准。

### 可移植性边界

代码和数据分家之后，跟机器绑定的东西只剩三处：`$MEM_HOME/config.json` 里的绝对路径、
`.index/` 里的 venv 和模型缓存、以及 hook 注册里写的代码根绝对路径。换机器就是
重跑一次 `mem init` + 把 `memory/` 拷过去。

**换语言语料要重标的参数**（默认值都是在中文语料上量出来的）：

- `MAX_DF_RATIO = 0.25`（`search.py`）：丢掉命中率过高的词片。中文 3-gram 的分布和
  英文单词很不一样，英文语料下这个值通常可以更低
- `recall.quota_min_sim = 0.30`：分层配额的入场分。本机实测相关查询 L1 最高分
  0.32~0.56、无关查询 ≤ 0.27，所以卡在 0.30。换模型（不同的各向异性）或换语言都要重量一遍：
  拿五六个"明确有答案"和五六个"明确无关"的问题跑一下 `semantic_scored`，取中间
- `audit.dup_similarity = 0.55` / `reconfirm_similarity = 0.85`：候选查重的阈值，
  同样是**去均值后**的余弦，尺度比原始余弦低得多。实测逐字重复 0.90、近义重复 0.56~0.63、
  无关条目 0.17~0.26。两个数分开定是因为代价不同：查重判错只是候选进 `rejected/`
  且报告里写明像谁，人能捞回来；而给一条其实已经过时的记忆盖上"刚验过"是真伤害，
  所以刷 `verified_at` 卡到近乎逐字重复才做
- `chunk_target_chars = 600`：中文 600 字 ≈ 英文 600 字符会短很多，英文语料建议翻倍

**不需要改的**：trigram + stride=1 的切法对英文同样成立（英文单词会被 `LATIN` 正则
整词抽出来走精确匹配，3-gram 只是补充）；去均值那一步对所有 bge 系模型都适用。

### 什么时候该推翻这套设计

- 块数超过 **10 万**：numpy 暴力检索开始吃力，换 sqlite-vec 或 HNSW。
- 需要**多机共享**：SQLite 单文件 + rsync 会冲突，得上服务端。
- 需要**结构化查询**（"上周所有关于 XX 的改动"）：现在只有全文和语义两条腿，
  没有按时间 / 项目 / 分支过滤的维度，元数据都塞在 `meta_json` 里没建索引。

---

## 8. 排查手册

| 症状 | 先看 |
|---|---|
| 检索不到刚写的记忆 | 没跑 `mem index`。索引是按 mtime 增量的，不会自动触发 |
| 结果里全是 `K-`，没有 `V` | 语义腿挂了。`mem doctor` 看"上次语义检索错误"；多半是模型缓存丢了或 venv 坏了 |
| 结果里全是 `-V`，没有 `K` | 查询里没有 df 落在 (0, 25%] 区间的词片，属正常——纯自然语言问法就是这样 |
| `没有命中` | 两条腿都空。看 `mem doctor` 的索引库块数；库是空的就 `mem index --full` |
| 检索很慢（>3s） | fastembed 在联网探测 HuggingFace。确认 `.index/models` 下有 `.onnx`，有则会自动切离线 |
| `记忆库运行时缺失` | venv 没了，按 `bin/mem` 的提示用 uv 重建，**必须走清华源** |
| 索引报维度不匹配 | 换过嵌入模型。`mem index --full` |
